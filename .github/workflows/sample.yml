name: Trigger All Git Flow from PR Comment

on:
    issue_comment:
        types: [created]

jobs:
    run-all:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            # - uses: octokit/request-action@v2.x
            #   id: branch-name
            #   with:
            #       route: GET /repos/{owner}/{repo}/pulls/${{ github.issue.number }}
            #       owner: carlo-setldpay
            #       repo: SampleWorkflow
            #   env:
            #       GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
            # - run: "echo latest release: '${{ steps.branch-name.outputs.data }}'"
            - id: sample
              run: |
                  response=$(curl -s 'https://api.github.com/repos/carlo-setldpay/SampleWorkflow/pulls/2')
                  echo 'data=$response' >> $GITHUB_OUTPUT
            - run: "echo latest release: '${{ steps.sample.outputs.data }}'"
